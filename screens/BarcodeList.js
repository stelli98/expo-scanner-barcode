import React from "react";
import { Text, View, StyleSheet } from "react-native";

const BarcodeList = () => {
  return (
    <View style={styles.container}>
      <Text>Barcode List Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default BarcodeList;
